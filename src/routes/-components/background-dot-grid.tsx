// Think we need to find the full page width and height then create circle divs to equally space out between them to fill the page

export default function BackgroundDotGrid() {
	return (
		<div className="h-full w-full bg-[radial-gradient(circle,#e4e4e7_3px,transparent_3px)] bg-[length:40px_40px]" />
	);
}
